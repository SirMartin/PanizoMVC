@if(Request.IsAuthenticated) {
    <text>Hola <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Salir", "LogOff", "Account") ]</text>
}
else {
    @: @Html.ActionLink("Acceder", "LogOn", "Account") |
    @: @Html.ActionLink("Registrate", "Register", "Account") 
}
